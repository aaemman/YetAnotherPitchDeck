<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Yapd" />

    <style name="Yapd.Text">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/gray7</item>
        <item name="android:lineSpacingMultiplier">1.3</item>
    </style>

    <style name="Yapd.Text.SectionHeader">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="Yapd.Text.Title">
        <item name="android:textSize">30sp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Yapd.Button">
        <item name="android:layout_height">36dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:minWidth">64dp</item>
        <item name="android:layout_marginTop">6dp</item>
        <item name="android:layout_marginBottom">6dp</item>
        <item name="android:paddingLeft">@dimen/standard_button_padding</item>
        <item name="android:paddingRight">@dimen/standard_button_padding</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:background">@drawable/transparent_to_gray_25_background</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="Yapd.Button.Raised">
        <item name="android:background">@drawable/widget_raised_button_color_primary_background
        </item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
